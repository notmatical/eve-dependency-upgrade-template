{
  "name": "eve-dependency-upgrade-template",
  "version": "0.0.0",
  "type": "module",
  "private": true,
  "imports": {
    "#*": "./agent/*",
    "#evals/*": "./evals/*"
  },
  "scripts": {
    "dev": "eve dev",
    "build": "eve build",
    "start": "eve start",
    "typecheck": "tsc --noEmit",
    "test": "bun test agent",
    "validate": "bun run typecheck && bun run test",
    "eval": "eve eval",
    "fixtures:check": "bun scripts/check-fixtures.ts",
    "fixtures:run": "bun scripts/run-fixtures.ts"
  },
  "dependencies": {
    "@github-tools/eve-extension": "0.5.0",
    "@vercel/connect": "^1.0.0",
    "eve": "0.47.3",
    "zod": "4.4.3"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "just-bash": "^3.4.2",
    "typescript": "7.0.2"
  },
  "engines": {
    "node": "24.x"
  }
}
